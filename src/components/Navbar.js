function Navbar(){
    return(
        <nav>
            <h1>Tacos</h1>
            <div>
                <a href="#Menu">Menu</a>
                <a href="#Pedir">Pedir</a>
                <a href="#footer">Footer</a>
            </div>
        </nav>
    );
};

export default Navbar;